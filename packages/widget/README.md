# anywidget-bundle

`anywidget-bundle` loads a self-contained Vite build into an anywidget model.

See the [documentation](https://peter-gy.github.io/anywidget-bundle/) for the build and Python APIs.
